<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".key_dates"
            android:label="@string/title_activity_key_dates"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".pending_claims"
            android:label="@string/title_activity_pending_claims"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".claim_submitted"
            android:label="@string/title_activity_claim_submitted"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".submit_claim"
            android:label="@string/title_activity_submit_claim"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".homePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>